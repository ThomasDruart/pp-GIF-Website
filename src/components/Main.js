import React from "react";
import styled from "styled-components";

const MainStyled = styled.div`
  height: 80vh;
  text-align: center;
`;

export default function Main() {
  return <MainStyled>Body</MainStyled>;
}
